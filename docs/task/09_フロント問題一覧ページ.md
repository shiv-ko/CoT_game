# 9. フロント問題一覧ページ

## 目的 / 背景

- `docs/app.md` ユーザー側機能: 問題の表示。
- API `/api/v1/questions` を UI で可視化し解答導線を提供。

## 完了条件

- ページ (Next.js App Router) `/questions` 作成。
- 初回マウント時に問題一覧取得→ローディング/エラー/空表示状態管理。
- 各行に「解く」ボタン（`/questions/[id]` 遷移）。
- ESLint/型エラーなし。

## スコープ

- 含む: fetch ラッパ利用, 型定義, UIコンポーネント。
- 含まない: ページネーション, 並び替え。

## 作業手順

- [x] `services/api.ts` に questions 取得関数
- [x] 型 `Question` 追加 (id, level, problem_statement, created_at)
- [x] `/src/app/questions/page.tsx` 実装
- [x] 状態: loading/error/success
- [ ] 単純なテスト (React Testing Library, optional)

## 実装済み追加機能

- レベルフィルター機能 (`LevelFilter.tsx`)
- コンポーネント分割 (LoadingState, ErrorState, EmptyState, QuestionsTable)
- 星マーク表示 (renderStars)
- リトライ機能付きエラーハンドリング

## 依存関係 / リスク

- 依存: questions API (既存済み)
- リスク: API 失敗時のUX → リトライボタン

## 見積り / 担当 / 期日

- 見積り: 0.5人日
- 担当: 未定
- 期日: Sprint2 (2025-09-18)

## 参考リンク

- `../app.md`
