# 11. フロント結果表示コンポーネント

## 目的 / 背景

- 送信結果を視覚的に表示し再挑戦導線を提供 (`docs/app.md` スコア可視化)。

## 完了条件

- 独立コンポーネント `<SolveResult />` (props: ai_output, score, answer_number, evaluation.mode, elapsed_ms)。
- スタイル: シンプルなカード表示。
- スコアを細かく色分け
- 再挑戦ボタン (親フォーム state リセットコールバック)。

## スコープ

- 含む: 表示ロジック, style (CSS / Tailwind)。
- 含まない: チャート, 履歴比較。

## 作業手順

- [x] 型定義 `SolveResultProps`
- [x] JSX + スタイル実装
- [x] スコア色分けロジック実装
- [x] 再挑戦ボタンコールバック
- [ ] Storybook 省略 / テスト任意

## 実装詳細

- `/questions/[id]/components/SolveResult.tsx` 実装完了
- Props: `result: SolveResponse`, `onRetry: () => void`
- スコア表示:
  - 90点以上: 緑 (#22c55e) - 「素晴らしい!」
  - 70点以上: 青 (#3b82f6) - 「よくできました!」
  - 50点以上: オレンジ (#f59e0b) - 「もう少し頑張りましょう」
  - 50点未満: 赤 (#ef4444) - 「再挑戦してみましょう」
- 表示内容:
  - スコア (大きく強調表示)
  - AIの回答 (pre-wrap, word-break)
  - 詳細情報 (モデル, 応答時間, 抽出数値, 評価モード, 保存状態)
- 再挑戦ボタン (親コンポーネントのstate リセット)

## 依存関係 / リスク

- 依存: フロント問題詳細送信フォーム
- リスク: API変更時のProps更新漏れ → 型定義集中管理

## 見積り / 担当 / 期日

- 見積り: 0.5人日
- 担当: 未定
- 期日: Sprint2 (2025-09-19)

## 参考リンク

- `../app.md`
