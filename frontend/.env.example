# -----------------------------
# Frontend (Next.js) Public Vars
# -----------------------------
# Base URL for calling backend from browser.
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Feature flags (examples – add as needed)
# NEXT_PUBLIC_ENABLE_LEADERBOARD=true
# NEXT_PUBLIC_SHOW_DEBUG_INFO=false

# -----------------------------
# Logging / Observability
# -----------------------------
# Log level: debug | info | warn | error  (currently not wired; placeholder for future logging pkg)
LOG_LEVEL=debug

# -----------------------------
# Rate Limiting / Security (future)
# -----------------------------
# RATE_LIMIT_PER_MIN=60
# CORS_ALLOWED_ORIGINS=http://localhost:3000

# -----------------------------
# Misc / Future Expansion
# -----------------------------
# APP_ENV=local            # local | staging | prod
# SENTRY_DSN=              # error tracking
# ENABLE_DB_MIGRATIONS=1   # gate automatic migrations at startup

# End of file.
