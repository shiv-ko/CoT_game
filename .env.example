# =============================
# CoT_game Environment Template
# =============================
# Copy this file to `.env` and fill in the required values.
# Lines starting with `#` are comments.
# UTF-8 encoding.
#
# Loading order:
# - backend/main.go uses github.com/joho/godotenv to read `.env` automatically.
# - For docker-compose, these values can be overridden by real env or secrets.
# - Frontend (Next.js) only exposes variables prefixed with NEXT_PUBLIC_ to the browser.
#
# Security:
# - NEVER commit a real API key or database password into .env (only this example file stays versioned).
# - Add `.env` to .gitignore (verify) so local secrets are not committed.

# -----------------------------
# Backend Core
# -----------------------------
# PostgreSQL connection URL. Format:
#   postgres://<user>:<password>@<host>:<port>/<database>?sslmode=disable
# Minimum required for backend to start.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/cot_game?sslmode=disable

# -----------------------------
# AI / LLM Providers
# -----------------------------
# Task #1 will implement Gemini client. Keep a placeholder for future multi-provider support.
# Required when AI features are invoked.
GEMINI_API_KEY=__REPLACE_ME__

# Optional: override API endpoint (advanced / testing)
# GEMINI_API_BASE=https://generativelanguage.googleapis.com/v1beta

# Model selection (future abstraction). Example values: gemini-1.5-flash, gemini-1.5-pro
AI_MODEL_NAME=gemini-1.5-flash

# -----------------------------
# Frontend (Next.js) Public Vars
# -----------------------------
# Base URL for calling backend from browser.
# Used as API_BASE_URL in frontend code (NEXT_PUBLIC_API_URL)
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# -----------------------------
# Rate Limiting / Security
# -----------------------------
# Rate limit per minute (future use for rate limiting middleware)
RATE_LIMIT_PER_MINUTE=60

# CORS allowed origins (future use)
# CORS_ALLOWED_ORIGINS=http://localhost:3000

# -----------------------------
# Environment Configuration
# -----------------------------
# Node.js environment: development | production | test
NODE_ENV=development

# Backend environment (future use): local | staging | prod
# APP_ENV=local

# -----------------------------
# Logging / Observability
# -----------------------------
# Log level: debug | info | warn | error (currently not wired; placeholder for future logging pkg)
LOG_LEVEL=debug

# -----------------------------
# Misc / Future Expansion
# -----------------------------
# HTTP listen port for backend (Gin). Not yet parameterized in code (default 8080).
# BACKEND_PORT=8080

# Timeout (seconds) for outbound AI requests (future use)
# AI_REQUEST_TIMEOUT=15

# Feature flags (examples – add as needed)
# NEXT_PUBLIC_ENABLE_LEADERBOARD=true
# NEXT_PUBLIC_SHOW_DEBUG_INFO=false

# Error tracking (future use)
# SENTRY_DSN=

# Gate automatic migrations at startup (future use)
# ENABLE_DB_MIGRATIONS=1

# End of file.
